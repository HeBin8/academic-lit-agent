# Model API Keys (optional - can also configure via Streamlit UI)
DEEPSEEK_API_KEY=sk-your-deepseek-key
MIMO_API_KEY=your-mimo-key

# Default model to use when starting the app
DEFAULT_MODEL=DeepSeek V3
